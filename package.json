{
  "name": "snip-backend",
  "version": "1.0.0",
  "description": "Snip – tiny URL shortener backend (Bun, zero dependencies)",
  "scripts": {
    "start": "bun run server.js"
  },
  "engines": {
    "bun": ">=1.0.0"
  }
}
